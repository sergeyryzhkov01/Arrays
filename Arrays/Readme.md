# Работа с массивами в C
## 📋 Задания к лаб. 11
1. **Заполнение массива с консоли**
2. **Печать массива**
3. **ДЗ (Вариант 7):** Вычисление количества элементов массива, расположенных до минимального элемента.
## 📋 Задания к лаб. 14
1. **Заполнение массива случайными значениями**
2. **Печать массива**
3. **ДЗ (Вариант 7):** Поиск минимального значения элементов массива в заданном интервале $(A, B)$.
## 📋 Задания к лаб. 16
1. **Генерация трех массивов ($A. B, C$) случайной длины (10...50).*
2. **ДЗ (Вариант 14):** Реализовать функцию, возвращающую новый массив $D$. *Структура массива D:*
- Все элементы массива $A$
- Элементы массива $B$ в случайном порядке.
- В конце - накопленная сумма элементов массива $C$
## 📋 Задания к лаб. 17
1. **Реализация функций сортировки массива целых чисел:**
   - Пузырьковая сортировка;
   - Шейкерная сортировка;
   - Сортировка простым выбором;
   - Сортировка вставками;
2. **Разработка программного комплекса для вычислительных экспериментов.**
   - Формирование случайного массива.
   - Замер времени выполнения сортировок с помощью 'clock()'.

## 📁 Структура проекта
```
lab-arrays/
├── src/
│   ├── arrays.h    
│   ├── arrays.c     
│   ├── app.c         
|   └── Readme.md
├── Makefile          
└── README.md         
```
## 🔧 Реализованные функции
1. ```int count_before_min(int* arr, int n); ```
   - **Назначение:** Подсчет количества элементов, стоящих перед минимальным элементом.
   - **Параметры:**
      - ``` arr ``` - указатель на целочисленный массив.
      - ```n``` - размер массива.
   - **Возвращает:** Индекс минимального элемента (что равно количеству элементов до него).
2. ```void fill_random_array(double* arr, int n, double min, double max);```
   - **Назначение:** Заполнение массива случайными вещественными числами.
   - **Параметры:**
       - ```arr``` - указатель на массив.
       - ```n``` - размер массива.
       - ```min, max``` - диапазон генерации.
3. ```double find_min_in_interval(double* arr, int n, double A, double B, int* isFound);```
   - **Назначение:** Поиск минимального значения среди элементов, попадающих в интервал (A, B).
   - **Параметры:**
       - ```arr``` - указатель на массив.
       - ```n``` - размер.
       - ```A, B``` - границы интервала.
       - ```isFound``` - указатель на флаг (записывает 1, если число найдено, иначе 0)
    - **Возвращает:** Найденное минимальное значение
4. ```double* create_merged_array(double* A, int n, double* B, int m, double* C, int l, int* sizeD);```
   - **Назначение:** Создание нового массива D по правилу *A + B(обр) + суммC*
   - **Параметры:**
       - ```A, B, C``` - исходные массивы.
       - ```n, m, l``` - размеры исходных массивов.
       - ```sizeD``` - указатель для записи размера нового массива.
    - **Возвращает:** Указатель на новый динамический массив D (требует освобождения памяти через ```free```)
5. ```void print_double_array(const char* name, double* arr, int n);```
   - **Назначение:** Вывод элементов вещественного массива в консоль.
   - **Параметры:**
       - ```name``` - имя массива
       - ```arr``` - указатель на массив.
       - ```n``` - размер.
    - **Возвращает:** Ничего (void).
6. ```int* full_array(int* ptrarr, int n);```
   - **Назначение:** Заполнение целочисленного массива случайными значениями (0..1000) для экспериментов.
   - **Параметры:**
       - ```ptrarr``` - указатель на массив.
       - ```n``` - размер массива.
   - **Возвращает:** Указатель на заполненный массив.
7. ```int put_array(int* ptrarr, int n);```
   - **Назначение:** Вывод элементов массива в консоль (с усечением вывода для больших массивов).
   - **Параметры:**
       - ```ptrarr``` - указатель на массив.
       - ```n``` - размер массива.
   - **Возвращает:** 0 (Код успеха).
8. ```void sort_bubble(int* ptrarr, int n);```
   - **Назначение:** Сортировка массива методом пузырька (обмен соседних элементов
   - **Параметры:**
       - ```ptrarr``` - указатель на сортируемый массив.
       - ```n``` - количество элементов.
9. ```void sort_kokteil(int* ptrarr, int n);```
    - **Назначение:** Шейкерная сортировка (проход в обоих направлениях).
    - **Параметры:**
       - ```ptrarr``` - указатель на сортируемый массив.
       - ```n``` - количество элементов.
10. ```void sort_select(int* ptrarr, int n);```
    - **Назначение:** Сортировка простым выбором (поиск минимума и обмен с текущим).
    - **Параметры:**
       - ```ptrarr``` - указатель на сортируемый массив.
       - ```n``` - количество элементов.
11. ```void sort_insert(int* ptrarr, int n);```
    - **Назначение:** Сортировка вставками (постепенное построение отсортированной последовательности).
    - **Параметры:**
       - ```ptrarr``` - указатель на сортируемый массив.
       - ```n``` - количество элементов.
         



